// backend/jobs.config
// Wix Velo Scheduled Jobs Configuration
// 將此內容複製到 Wix 後端的 backend/jobs.config

{
  "jobs": [
    {
      "functionLocation": "/cleanupSessions.jsw",
      "functionName": "cleanupIdleSessions",
      "description": "Clean up idle chat sessions every 5 minutes",
      "executionConfig": {
        "cronExpression": "*/5 * * * *"
      }
    }
  ]
}
